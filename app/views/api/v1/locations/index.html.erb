<p id="notice"><%= notice %></p>

<h1>Listing Locations</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @resources.each do |location| %>
      <tr>
        <td><%= location.name %></td>
        <td><%= link_to 'Show', api_location_path(location) %></td>
        <td><%= link_to 'Edit', edit_api_location_path(location) %></td>
        <td><%= link_to 'Destroy', api_location_path(location), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Location', new_api_location_path %>
